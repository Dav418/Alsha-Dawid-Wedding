/// Copy for the map page — change [menuLabel], [pageTitle], or [pageSubtitle] here.
abstract final class MapPageConfig {
  MapPageConfig._();

  /// Burger menu label (drawer shows this in ALL CAPS).
  static const menuLabel = 'MAP OF THE AREA';

  /// Script hero title at the top of the page.
  static const pageTitle = 'Map of the Area';

  /// Caps subtitle under the hero title.
  static const pageSubtitle = 'NEARBY ACCOMMODATION & PLACES TO VISIT';
}
